use prueba
go top
c=reccount()
do while .t.
    b=recno()
    if b=c
        exit
    endif
    a=tc
    if a<>"A"
        if a<>"B"
            delete
        endif
    endif
    skip
    loop
enddo
packç
